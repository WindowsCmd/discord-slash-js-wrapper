const axios = require("axios");

class client {
  /**
   * Discord bot token
   * @param {string} token
   *
   * Your app id
   * @param {strig} appId
   */
  constructor(token, appId) {
    this.token = token;
    this.appId = appId;
  }
}
